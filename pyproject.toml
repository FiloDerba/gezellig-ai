[project]
name = "gezellig-ai"
version = "0.1.0"
description = "A Dutch spaced-repetition trainer with AI coaching, plus a pydantic-ai playground"
readme = "README.md"
authors = [
    { email = "filippo.alberton95@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "altair>=5.5.0",
    "django>=6.1.1",
    "logfire>=4.40.0",
    "pandas>=2.3.0",
    "psycopg[binary]>=3.2.12",
    "pydantic-ai-slim[anthropic,google,openai]>=2.32.1",
    "python-dotenv>=1.2.3",
    "streamlit>=1.63.0",
    "tavily-python>=0.7.27",
]

[project.scripts]
gezellig-ai = "ai_playground:main"

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
# The project is "gezellig-ai" but the packages under src/ are not, so the backend has to
# be told which modules to ship instead of inferring one from the project name.
module-name = ["ai_playground", "dutch_learning"]
namespace = true

[dependency-groups]
dev = [
    "pre-commit>=4.4.0",
    "pyright>=1.1.411",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-django>=4.14.0",
    "ruff>=0.16.3",
]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
# "vocabulary" and "backend" live under django/, not src/, so name them explicitly.
known-first-party = ["ai_playground", "dutch_learning", "backend", "vocabulary"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "django"]
pythonpath = ["src", "django"]
DJANGO_SETTINGS_MODULE = "backend.settings"
python_files = ["test_*.py"]
python_classes = ["Test*", "*Tests"]

[tool.pyright]
include = ["src", "tests"]
venvPath = "."
venv = ".venv"
